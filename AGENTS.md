# CMIOT-rag Agent Memory

这份文件用于沉淀当前项目里容易反复踩坑、且适合作为执行前记忆的经验。开始新任务前，优先先过一遍这里的内容。

## Windows PowerShell 中文编码坑

在 Windows PowerShell 里直接手写中文字符串、中文标签，尤其是通过内联命令、内联 Python、一次性脚本去生成 Markdown、JSON、日志、测试报告时，中文内容有概率在输入阶段就被编码成 `?`。

这个问题在当前项目里已经多次出现过，典型表现包括：

- 测试问题本身被写成 `????`
- 报告标题、字段标签、Markdown 文案被写成 `????`
- `rewrite_query` 看起来像模型重写坏了，但实际是原始输入在进入后端前就已经损坏

### 执行约束

- 不要在 PowerShell 内联命令里直接手写中文测试题目、中文报告标签、中文固定文案。
- 优先从 `json`、`docx`、数据库、源码文件等 UTF-8 来源读取中文内容。
- 需要在脚本里固定中文时，优先放进 `.py` 文件，或使用 Unicode 转义，不要直接塞进终端命令。
- 同时生成 `json` 和 `md` 时，优先以 `json` 作为真源，再从 `json` 派生 `md`。
- Windows 下写报告、日志、导出文件时，优先使用 `utf-8-sig`，降低再次打开出现乱码的概率。

### 排查顺序

如果出现中文问号乱码，按这个顺序排查：

1. 先检查终端输入阶段是否已经损坏。
2. 再检查中间脚本是否把中文标签写坏。
3. 最后再怀疑后端检索、重写、回答链路。

不要一上来就判断是模型理解或 RAG 逻辑出了问题。
