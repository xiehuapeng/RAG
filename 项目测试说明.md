# 项目测试说明

这份文档用于说明当前仓库里已经存在的测试方式、执行入口和适用场景。按当前实现，测试分成两类：

1. 自动化回归测试：后端基于 `pytest`，前端基于 Node.js 内置测试器，验证服务层和页面状态处理逻辑。
2. 输入数据驱动的问答评测：批量调用真实问答接口，对回答做规则评分或 LLM 裁判评分。

## 1. 测试总览

| 类型 | 入口 | 是否依赖已启动后端 | 主要用途 |
|------|------|------------------|---------|
| 自动化回归测试 | `scripts/test-backend.ps1` / `pytest tests -q` | 否 | 验证问题理解、检索门槛、流式进度、兜底回答、日志输出 |
| 前端回归测试 | 前端目录 `npm test` | 否 | 批量删除并发限制、部分失败、取消、防重复及真实页面状态处理 |
| 规则评分批量评测 | `tests/qa_eval/run_qa_eval_reference_with_progress.py` | 是 | 批量发问并按人工整理的关键点覆盖率评分 |
| LLM 裁判批量评测 | `tests/qa_eval/run_backend_qa_and_llm_judge_merged.py` | 是 | 批量发问后再用外部大模型裁判答案质量 |
| 报告二次裁判 | `tests/qa_eval/judge_qa_report_with_model_sequential*.py` | 否，但需要已有报告 | 对已有 `qa_test_report.json` 追加 LLM 裁判结果 |

## 2. 自动化回归测试

### 2.1 推荐入口

在仓库根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-backend.ps1 -Install
```

如果后端依赖已经装好，可以省略 `-Install`：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-backend.ps1
```

这个脚本会：

1. 进入 `knowledge_base_backend`
2. 使用 `.venv\Scripts\python.exe`
3. 执行 `python -m pytest tests -q`
4. 依赖安装失败立即停止；测试失败向调用进程返回非零退出码，避免 CI 假成功。

如果你已经在后端目录，也可以直接运行：

```powershell
cd .\knowledge_base_backend
.\.venv\Scripts\python.exe -m pytest tests -q
```

### 2.2 当前覆盖内容

当前 `pytest` 测试文件都在 [knowledge_base_backend/tests](knowledge_base_backend/tests)。

- [test_query_understanding.py](knowledge_base_backend/tests/test_query_understanding.py)
  - 强关键词命中是否直接走 `kb_qa`
  - 超范围问题是否被规则拦截
  - 追问是否继承上一轮主题
  - 低置信度模型结果是否回退到规则结果
- [test_chat_stream_progress.py](knowledge_base_backend/tests/test_chat_stream_progress.py)
  - 流式问答是否按预期输出 `start -> understanding -> retrieving -> filtering -> generating -> completed`
  - 超范围时是否把 RAG 步骤标成 `skipped`
- [test_chat_fallback.py](knowledge_base_backend/tests/test_chat_fallback.py)
  - 无关问题是否返回“知识库未命中”的兜底答案
  - 相关问题是否保留引用摘要
- [test_chat_observability_logs.py](knowledge_base_backend/tests/test_chat_observability_logs.py)
  - 问题理解日志是否只记录源问题和改写后问题
  - 检索日志和证据日志是否只记录必要摘要
  - 单题日志文件是否按问题名落盘
- [test_retrieval_gate.py](knowledge_base_backend/tests/test_retrieval_gate.py)
  - 检索相关性门槛是否按预期放行或拦截

### 2.3 当前特点与边界

- 这部分测试偏单元测试和服务层回归。
- 大量使用 mock / fake DB，不依赖真实后端服务启动。
- 外部 HTTP 由自动 fixture 拦截；规则测试显式 mock 模型补充，日志放在临时目录，覆盖上传测试使用内存 SQLite 和假向量存储。
- 检索门槛测试已经指向当前 `retrieval_langchain.py`，不再只测旧实现。
- 不会覆盖真实知识库内容质量，也不会批量验证回答是否符合业务手册。

### 2.4 新增故障回归

- `test_document_replacement.py`：解析/空内容/向量部分写入/数据库提交失败不丢旧文档，成功切换和旧资源清理失败边界。
- `test_reranker.py`：最终得分优先、来源配额去重、结构块拦截、模型加分/失败降级、追问改写 query。
- `test_llm_client.py`：HTTP 错误无隐式重试、超时传递、空/截断响应拒绝、主备共享剩余调度预算。
- `test_test_runner.py`：使用没有 pip/pytest 的临时虚拟环境验证 PowerShell 的安装/测试失败退出码，不改动实际运行环境。

### 2.5 前端回归与构建

在 `knowledge_base_frontend` 目录运行 `npm test` 和 `npm run build`。无需新增测试依赖：使用 Node 内置 `node:test` 和 Vue 自带的 SFC 编译器。

- `tests/sessionBatch.test.js`：全部成功/部分失败/全部失败、404 幂等处理、去重及最多 4 个并发请求。
- `tests/chatBatchState.test.js`：执行实际 `ChatPage.vue` 的处理函数，验证删除后刷新、失败项保留、活动会话清理、取消及防重复；IO 被 mock，不代表浏览器端到端验收。

### 2.6 真实服务冒烟

前后端启动后，在仓库根目录执行：

```powershell
.\knowledge_base_backend\.venv\Scripts\python.exe .\scripts\smoke-runtime.py
```

脚本使用 `QA_EVAL_USERNAME` / `QA_EVAL_PASSWORD`（未设置时使用项目开发默认账户），验证健康检查、真实文档解析和向量入库、成功替换、损坏文件覆盖保护，以及带引用的 SSE 问答。它会创建随机命名的虚构测试文档和会话，并在结束时只删除自己创建的记录、退出登录；清理失败会返回非零退出码并列出测试记录 ID，不打印 token 或回答正文。

这是真实集成测试，会写入临时业务记录并调用已配置模型，可能产生费用；不要与业务效果评测并行运行，避免评测检索到临时文档。可用 `--skip-qa` 跳过 LLM/SSE，但文档上传仍会使用真实 embedding 和 Chroma。

2026-09-08 验证记录：后端 45 项回归通过（21.96 秒），前端 9 项回归通过，生产构建成功；真实 SSE 单次耗时 19.76 秒，90 个 delta、160 字符回答、12 条引用且包含测试来源。该次本地 reranker 对 16 个候选耗时 7.79 秒（包含首次加载，非稳定性能基准）。临时文档和会话已清理，原文档、切片、会话、消息四张表逐行摘要与运行前备份一致。浏览器登录和问答页可访问，控制台无错误；390px 视口无横向溢出。

这些结果证明链路可用，不等同于业务问答准确率提升；固定问题集 rerank 开关 A/B、稳定延迟分位数，以及普通重建/单块编辑的跨存储一致性仍需专项验证。

## 3. 输入数据驱动的问答评测

这部分脚本都在 [knowledge_base_backend/tests/qa_eval](knowledge_base_backend/tests/qa_eval)，不属于 `pytest` 自动回归的一部分。

### 3.1 规则评分批量评测

当前实际可运行的脚本入口是：

- [run_qa_eval_reference_with_progress.py](knowledge_base_backend/tests/qa_eval/run_qa_eval_reference_with_progress.py)

执行前要求：

1. 后端服务已启动
2. `admin / admin123` 可用，或你已通过环境变量覆盖账号密码

推荐运行方式：

```powershell
.\knowledge_base_backend\.venv\Scripts\python.exe .\knowledge_base_backend\tests\qa_eval\run_qa_eval_reference_with_progress.py
```

也可以指定案例文件和输出目录：

```powershell
.\knowledge_base_backend\.venv\Scripts\python.exe .\knowledge_base_backend\tests\qa_eval\run_qa_eval_reference_with_progress.py `
  --cases .\knowledge_base_backend\tests\qa_eval\qa_test_cases_3_first20.json `
  --output-dir .\knowledge_base_backend\tests\qa_eval\reports
```

支持的环境变量：

- `QA_EVAL_BASE_URL`
- `QA_EVAL_USERNAME`
- `QA_EVAL_PASSWORD`
- `QA_EVAL_TIMEOUT_SECONDS`

支持的常用参数：

- `--cases`
- `--output-dir`
- `--fail-on-error`
- `--reuse-session`
- `--non-strict-fatal`

#### 评测流程

1. 登录后端 `/api/auth/login`
2. 为每个 case 创建会话，或在 `--reuse-session` 下复用一个会话
3. 调用非流式问答接口 `/api/chat/sessions/{session_id}/messages`
4. 读取 `data.content` 作为 `actual_answer`
5. 用 case 中的关键点做规则评分
6. 输出报告到 `reports/`

#### 当前脚本实际使用的用例格式

这个脚本要求的字段是：

```json
{
  "id": "qa_001",
  "name": "简短名称",
  "question": "用户问题",
  "must_have_points": ["必须覆盖的核心点"],
  "nice_to_have_points": ["可选加分点"],
  "fatal_mistakes": ["出现即判严重错误的表述"],
  "reference_answer": "人工整理的参考答案",
  "threshold": 0.67,
  "category": "业务分类",
  "source_basis": "依据来源"
}
```

说明：

- `must_have_points` 决定核心通过率。
- `nice_to_have_points` 只做补充加分。
- `fatal_mistakes` 在严格模式下会直接拉低通过结果。
- `reference_answer` 主要用于人工复核和后续 LLM 裁判链路。

#### 当前默认输入与输出

- 默认案例文件：`qa_test_cases_3_first20.json`
- 默认输出目录：`reports`
- 输出文件：
  - `qa_test_report.json`
  - `qa_test_report.csv`

### 3.2 LLM 裁判批量评测

当前仓库里有一个“问答生成 + LLM 裁判”合并脚本：

- [run_backend_qa_and_llm_judge_merged.py](knowledge_base_backend/tests/qa_eval/run_backend_qa_and_llm_judge_merged.py)

它的流程是：

1. 调后端接口生成 `actual_answer`
2. 把 `question / source_basis / reference_answer / actual_answer` 发给外部裁判模型
3. 让裁判模型输出 `judge_passed / judge_score / factual_conflict / major_missing_points / major_errors / brief_reason`
4. 持续写入 judged 报告

当前这个脚本不是走命令行参数，而是通过文件头常量配置：

- `CASES_PATH`
- `KB_BASE_URL`
- `KB_USERNAME`
- `KB_PASSWORD`
- `JUDGE_BASE_URL`
- `JUDGE_API_KEY`
- `JUDGE_MODEL`

因此运行前要先检查脚本头部配置，不要直接把本地敏感配置提交回仓库。

运行方式：

```powershell
.\knowledge_base_backend\.venv\Scripts\python.exe .\knowledge_base_backend\tests\qa_eval\run_backend_qa_and_llm_judge_merged.py
```

默认输出目录：

- `reports_llm_judge`

默认输出文件：

- `qa_test_report_llm_judged.json`
- `qa_test_report_llm_judged.csv`

#### 当前常用输入文件

- [qa_test_case_4.json](knowledge_base_backend/tests/qa_eval/qa_test_case_4.json)
  - 更偏“参考答案 + 问题”的 LLM judge 用例
- [qa_test_cases_3_first20.json](knowledge_base_backend/tests/qa_eval/qa_test_cases_3_first20.json)
  - 更偏规则评分型批量评测
- [qa_test_cases.json](knowledge_base_backend/tests/qa_eval/qa_test_cases.json)
  - 旧版关键词格式，仍可作为历史样例参考，但不是 `run_qa_eval_reference_with_progress.py` 当前默认消费的格式

### 3.3 对已有报告追加 LLM 裁判

如果你已经先跑出了 `qa_test_report.json`，还可以用顺序裁判脚本做第二阶段评测：

- [judge_qa_report_with_model_sequential.py](knowledge_base_backend/tests/qa_eval/judge_qa_report_with_model_sequential.py)
- [judge_qa_report_with_model_sequential_v3.py](knowledge_base_backend/tests/qa_eval/judge_qa_report_with_model_sequential_v3.py)
- [judge_qa_report_with_model_sequential_v4_utf8.py](knowledge_base_backend/tests/qa_eval/judge_qa_report_with_model_sequential_v4_utf8.py)

这类脚本的输入是已有的 `reports/qa_test_report.json`，输出通常落到：

- `reports/judge_reports`
- 或脚本指定的其它 judged 目录

其中 `v4_utf8` 版本对 Windows 中文编码处理更稳，更适合当前项目环境。

## 4. 当前目录里的报告产物

当前仓库里已经有历史评测输出，可作为格式参考：

- [reports](knowledge_base_backend/tests/qa_eval/reports)
- [reports_llm_judge](knowledge_base_backend/tests/qa_eval/reports_llm_judge)

它们属于评测产物，不是自动化测试入口本身。

## 5. 当前已知注意点

### 5.1 `qa_eval` 文档和脚本曾经不一致

历史上 `qa_eval/README.md` 写过 `run_qa_eval.py`，但当前仓库里实际存在并可运行的是：

- `run_qa_eval_reference_with_progress.py`

后续如果再新增脚本，应优先同步更新文档，避免“README 写一个、目录里是另一个”。

### 5.2 用例 JSON 格式并不完全统一

当前仓库里至少存在 3 类评测数据格式：

1. 旧版关键词命中型：`expected_keywords / optional_keywords`
2. 规则评分增强型：`must_have_points / nice_to_have_points / fatal_mistakes`
3. 参考答案裁判型：`question / reference_answer / source_basis`

新增评测脚本时，要明确说明它消费的是哪一种格式。

### 5.3 Windows 中文编码问题

在 Windows PowerShell 里直接手写中文测试题目、中文报告标题、中文字段标签，可能会在输入阶段就被编码成 `?`。

建议统一遵循：

- 优先从 UTF-8 JSON 文件读取中文问题，不要直接在终端命令里手写。
- 报告文件优先使用 `utf-8-sig` 输出。
- 如果 `question`、`rewrite_query`、报告标题突然变成 `????`，先排查终端输入编码，而不是先怀疑 RAG 逻辑。

## 6. 建议使用顺序

如果目的是做日常开发回归，建议顺序如下：

1. 先跑自动化回归测试 `scripts/test-backend.ps1`
2. 需要验证真实问答效果时，再跑 `run_qa_eval_reference_with_progress.py`
3. 需要更贴近人工判断时，再补跑 LLM 裁判脚本

这样可以把“代码逻辑回归”和“知识问答效果评测”分开处理，定位问题会更快。
